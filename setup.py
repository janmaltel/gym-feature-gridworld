from setuptools import setup

setup(name='gym_feature_gridworld',
      version='0.0.2',
      packages=['gym_feature_gridworld'],
      install_requires=['gym', 'numpy']  # And any other dependencies foo needs
)
